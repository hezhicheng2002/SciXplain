# Public Crawling Helpers

This directory contains the public part of the SciXplain data pipeline.

- `build_article_manifest.py`: normalize input article metadata into a JSONL manifest
- `download_arxiv_pdfs.py`: download PDFs from arXiv-compatible entries in the manifest

These scripts are meant for local reconstruction only. They do not redistribute SciStruct or any copyrighted benchmark asset.

